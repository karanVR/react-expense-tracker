import React from 'react'

const Header = () => {
    return (
        <div>
            <h2 style={{display: 'block'}}>Expense-Tracker</h2>
            <h2>using hooks( useReducer + useContext )</h2>
        </div>
    )
}

export default Header
